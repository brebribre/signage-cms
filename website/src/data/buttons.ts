/** The hero's two button looks, so every button on the site is the same shape. Sizes are the
 *  caller's: these carry only the shape, colour and hover. */
export const PRIMARY = 'rounded-lg bg-action font-medium text-white transition-colors hover:bg-action-hover'
export const SECONDARY = 'rounded-lg bg-white/80 font-medium text-ink ring-1 ring-ink/70 backdrop-blur transition-colors hover:bg-ink hover:text-white'
