"""SQLModel tables.

Every table module must be imported here: Alembic's autogenerate walks
`SQLModel.metadata`, and a table nobody imported is invisible to it.

Phase 2 adds Account, User, DeviceAccess, Media, Playlist, PlaylistItem and Device.
"""
