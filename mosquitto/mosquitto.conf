# Local/dev broker only — see docker-compose.yml. No auth, no TLS: this is for prototyping
# the publish-on-change flow against localhost, not a deployment config. A real deployment
# needs per-device credentials and TLS before anything real talks to it.
listener 1883
allow_anonymous true

persistence true
persistence_location /mosquitto/data/
log_dest stdout
